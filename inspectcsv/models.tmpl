// Generated by gormcsv.
package importcsv

import (
	{{if .UsesTime}}"time"

	{{end}}
	"gorm.io/gorm"
)

// MakeModels returns the generated models.
func MakeModels() ModelFactory {
	return NewModelFactory(map[string]func() any{ {{range $k, $v := .Models}}
		"{{$k}}": func() any { return &{{$v}}{} },{{end}}
	})
}

{{range $k, $v := .Fields}}
type {{ $k }} struct {
   gorm.Model
   {{ range $f := $v }}{{$f.Name}} {{$f.Type}} {{$f.Tag}}
   {{end}}
}
{{end}}
